package com.laetienda.utils.service.test;

public interface WebappTest {
    WebappTest setPort(Integer port);
    WebappTest setPort(String port);
    WebappTest setAdmuserPassword(String password);
}
